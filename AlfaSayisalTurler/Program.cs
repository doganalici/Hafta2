using System;

namespace AlfaSayisalTurler
{
    class Program
    {
        static void Main(string[] args)
        {
            bool durum = true;
            bool durum2 = false;

            #region katlanabilir alan
            //Ctrl+K+C : seçili kodu açıklama satır(ları)ı haline getirir.
            //Console.WriteLine("Bool minimum değer = "+bool.Min);
            //Ctrl+K+U : seçili kodun açıklama satır(ları)ı iptal eder.
            //Console.WriteLine("MERHABA");
            /*
             * çok
             * satırlı
             * açıklama için kullanılır
             */
            #endregion


            //char karakter = 'a';
            //Console.WriteLine(karakter);
            //Console.WriteLine(karakter+1);
            //Console.WriteLine((int)karakter);
            //Console.WriteLine((char)65);

            var a = 12;
            var b = 4.3;
            var c = "Adem";
            var d = false;
            var e = 'k';

            Console.ReadKey();
        }
    }
}
